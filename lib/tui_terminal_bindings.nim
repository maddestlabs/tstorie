## Auto-generated bindings for tui_terminal module
## This file is generated by the autoExpose macro system

import ../nimini/runtime
import tui_terminal
import ../src/types, ../src/layers  # Import actual type definitions

proc initTUITerminalModule*(defaultLayer: ptr Layer, appState: ptr AppState) =
  ## Register all tui_terminal functions with nimini runtime
  ## This is called automatically during module initialization
  
  # Register global references (following figlet pattern)
  registerTUITerminalBindings(defaultLayer, appState)
  
  # Call auto-generated registration functions (created by autoExpose pragma)
  # These are generated in tui_terminal.nim at compile time
  register_button()
  register_label()
  register_isHovered()
  register_isActive()
